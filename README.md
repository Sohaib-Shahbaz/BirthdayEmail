# BirthdayEmail
A Python automation project that reads birthdays from a CSV file, selects a random pre-written birthday letter template, personalises it with the recipient’s name, and sends the email automatically via Gmail SMTP when a birthday matches today’s date.
